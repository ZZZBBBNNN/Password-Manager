apiVersion: v1
kind: Secret
metadata:
  name: auth-server-secrets
type: Opaque
data:
  # 这些值需要使用 base64 编码后填写
  db-url: ${BASE64_DB_URL}
  redis-url: ${BASE64_REDIS_URL}
  jwt-secret: ${BASE64_JWT_SECRET}
  encryption-key: ${BASE64_ENCRYPTION_KEY}